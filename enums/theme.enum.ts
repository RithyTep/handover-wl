export enum Theme {
	DEFAULT = "default",
	CHRISTMAS = "christmas",
	PIXEL = "pixel",
	LUNAR = "lunar",
	CODING = "coding",
	CLASH = "clash",
}

export const ThemeValues = Object.values(Theme) as [Theme, ...Theme[]]
