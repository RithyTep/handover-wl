export enum FeedbackType {
  BUG = "bug",
  FEEDBACK = "feedback",
  SUGGESTION = "suggestion",
  FEATURE = "feature",
}

export enum FeedbackStatus {
  NEW = "new",
  REVIEWED = "reviewed",
  RESOLVED = "resolved",
  DISMISSED = "dismissed",
}
