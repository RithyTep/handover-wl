# =============================================================================
# Jira Handover Dashboard - Environment Configuration
# =============================================================================
# This file serves as a template for environment variables.
# Copy this file to .env.local for local development or configure these
# variables in your deployment platform (Railway, Vercel, etc.)
#
# Twelve-Factor App Methodology:
# - Factor III: Store config in the environment
# - Factor IV: Treat backing services as attached resources
# =============================================================================

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# Log level: debug | info | warn | error
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Database Configuration (Factor IV: Backing Service)
# -----------------------------------------------------------------------------
# Neon PostgreSQL connection string (pooled connection)
# Get this from your Neon project dashboard
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Direct/unpooled connection URL (for migrations)
# DATABASE_URL_UNPOOLED=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# -----------------------------------------------------------------------------
# Jira Configuration (Factor IV: Backing Service)
# -----------------------------------------------------------------------------
# Your Atlassian instance URL (e.g., https://your-company.atlassian.net)
JIRA_URL=https://your-instance.atlassian.net

# Email associated with your Atlassian account
JIRA_EMAIL=your-email@example.com

# Jira API Token (generate at https://id.atlassian.com/manage/api-tokens)
JIRA_API_TOKEN=your-jira-api-token

# -----------------------------------------------------------------------------
# Slack Configuration (Factor IV: Backing Service)
# -----------------------------------------------------------------------------
# Slack Bot OAuth Token (starts with xoxb-)
# Create a Slack app at https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack User OAuth Token (starts with xoxp-)
# Optional: Required for posting as a user
SLACK_USER_TOKEN=xoxp-your-user-token

# Slack Webhook URL (optional, for simple integrations)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/xxx/xxx

# Default Slack channel ID (starts with C)
SLACK_CHANNEL=C0123456789

# Alternative: Channel ID
# SLACK_CHANNEL_ID=C0123456789

# -----------------------------------------------------------------------------
# AI Configuration (Optional)
# -----------------------------------------------------------------------------
# Groq API Key for AI-powered features
# GROQ_API_KEY=gsk_your-groq-api-key

# OpenAI API Key (alternative to Groq)
# OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# Scheduler Configuration
# -----------------------------------------------------------------------------
# Enable/disable scheduled tasks (true/false)
SCHEDULE_ENABLED=false

# =============================================================================
# Production Deployment Notes
# =============================================================================
#
# Vercel + Neon Deployment:
# 1. Create a Neon project at https://neon.tech
# 2. Copy the pooled connection string to DATABASE_URL
# 3. Copy the direct connection string to DATABASE_URL_UNPOOLED
# 4. Set all required environment variables in Vercel dashboard
# 5. Connect your GitHub repo to Vercel for auto-deployments
#
# Neon Connection Strings:
# - Pooled (DATABASE_URL): Use for application queries
# - Direct (DATABASE_URL_UNPOOLED): Use for Prisma migrations
#
# Security Checklist:
# - Never commit .env or .env.local files
# - Rotate API tokens regularly
# - Use separate credentials for development/staging/production
# - Neon connections are SSL by default
#
# =============================================================================
